import XCTest

/// Placeholder — real tests added in Phase 7 (SpacedRepetitionTests, CSVImportTests).
final class PlaceholderTests: XCTestCase {
    func testPlaceholder() {
        // Passes unconditionally until Phase 7 tests are added
        XCTAssertTrue(true)
    }
}
